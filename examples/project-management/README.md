# HyDB project management demo

An intentionally front-end-only SolidJS prototype that demonstrates HyDB's
in-memory storage, Zod-validated commands, nested query materialization, and
incremental subscriptions.

From the repository root:

```sh
npm run demo:project-management
```

Open the printed local URL. Data resets whenever the page reloads.
