{
  "name": "@hyos/hyos",
  "private": true,
  "version": "0.0.0",
  "description": "HyOS Electron shell with typed process-split modules",
  "main": "main.js",
  "scripts": {
    "build:hydb": "npm run build --workspace @hyos/hydb",
    "build:ui-agent": "npm run build --workspace @hyos/ui-agent",
    "predev": "npm run build:ui-agent",
    "dev": "electron .",
    "presmoke": "npm run build:ui-agent",
    "smoke": "electron . --smoke-test",
    "test": "node --test *.test.js && npm run build:hydb && tsx --test capabilities/*.test.ts modules/agent-main/*.test.ts modules/agent-renderer/*.test.ts modules/reload-main/*.test.ts",
    "typecheck": "npm run build:hydb && tsc --noEmit -p tsconfig.json",
    "check": "node --check main.js && node --check application-loader.js && node --check isomorphic-compiler.js && node --check runtime.js && node --check remote-capabilities.js && node --check preload.js && node --check renderer/renderer.js && npm run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^38.0.0",
    "esbuild": "^0.25.0",
    "esbuild-plugin-solid": "^0.6.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.258",
    "@hyos/hydb": "^0.0.0",
    "@hyos/ui-agent": "0.0.0",
    "@openai/codex-sdk": "^0.152.1",
    "mermaid": "^11.17.2",
    "micromark": "^4.0.2",
    "solid-js": "^1.9.15",
    "zod": "^4.4.3"
  }
}
