{
  "name": "hyos",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "demo:hyos": "npm run dev --workspace @hyos/hyos",
    "demo:project-management": "npm run dev --workspace @hyos/hydb-project-management-example",
    "dev": "npm run build --workspace @hyos/ui-agent && npm run dev --workspace @hyos/components",
    "dev:ui-agent": "npm run sandbox --workspace @hyos/ui-agent",
    "dev:hyagent": "npm run dev --workspace @hyos/hyagent",
    "dev:notes": "npm run dev --workspace @hyos/hy-notes",
    "build": "npm run build --workspace @hyos/ui-agent && npm run build --workspace @hyos/components",
    "typecheck": "npm run typecheck --workspace @hyos/ui-agent && npm run typecheck --workspace @hyos/components",
    "test": "npm run test --workspaces --if-present",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{css,js,jsx,json,md,ts,tsx,yaml,yml}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.4",
    "prettier": "^3.6.2"
  }
}
